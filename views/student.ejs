<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <div class="container">
            <h1>75YALMezunları</h1>
            <form action="/search" method="get" class="mezun-search">
                <input type="text" name="q" placeholder="Mezun adı veya numarası" required>
                <button type="submit">Ara</button>
            </form>
            <nav>
                <ul>
                    <li><a href="/">Ana Sayfa</a></li>
                    <li><a href="/hakkinda">Hakkında</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <div class="breadcrumb">
        <a href="/">Ana Sayfa</a>
        <span>></span>
        <a href="/mezunlar/<%= year %>"><%= year %> Mezunları</a>
        <span>></span>
        <a href="/mezunlar/<%= year %>/<%= className %>"><%= className %></a>
        <span>></span>
        <span><%= student.name %></span>
    </div>

    <main class="student-profile">
        <div class="profile-container">
            <div class="profile-image">
                <img src="<%= student.image %>" alt="<%= student.name %>">
            </div>
            <div class="profile-content">
                <h1><%= student.name %></h1>
                <p class="bio"><%= student.bio %></p>
                
                <section class="comments">
                    <h2>Yorumlar</h2>
                    <!-- Yorum ekle formu -->
                    <form action="/yorum-ekle" method="post" class="add-comment-form" style="margin-bottom: 24px; display: flex; flex-direction: column; gap: 10px; max-width: 500px;">
                        <input type="hidden" name="year" value="<%= year %>">
                        <input type="hidden" name="className" value="<%= className %>">
                        <input type="hidden" name="studentNumber" value="<%= student.number %>">
                        <input 
                            type="text" 
                            name="author" 
                            placeholder="Adınız" 
                            required 
                            style="padding: 8px 12px; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem;"
                        >
                        <textarea 
                            name="text" 
                            placeholder="Yorumunuz" 
                            required 
                            style="padding: 8px 12px; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem; min-height: 60px; resize: vertical;"
                        ></textarea>
                        <div style="display: flex; align-items: center; gap: 12px;">
                            <button type="submit" class="btn" style="padding: 8px 24px; font-size: 1rem;">Yorum Ekle</button>
                            <span style="font-size:0.95em;color:#888;">Yorumunuz önce yöneticinin onayına gönderilecektir.</span>
                        </div>
                    </form>
                    <div class="comment-list">
                        <% student.comments.forEach(comment => { %>
                        <div class="comment">
                            <p class="comment-text"><%= comment.text %></p>
                            <div class="comment-meta">
                                <span class="author">Yazan: <%= comment.author %></span>
                                <span class="date"><%= comment.date %></span>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                </section>
            </div>
        </div>
    </main>
</body>
</html>